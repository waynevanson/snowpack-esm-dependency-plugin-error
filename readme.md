Replicate bug by doing the following:

```bash
git clone https://github.com/waynevanson/snowpack-esm-dependency-plugin-error.git
cd snowpack-esm-dependency-plugin-error
pnpm install
cd example-application
pnpm snowpack build
```
